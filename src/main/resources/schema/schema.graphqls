type Query {
    students(nameFilter: String): [Student]
}

type Student {
    id: ID
    name: String
    age: Int
    school: School
}

type School {
    id: ID
    name: String
    address: String
}